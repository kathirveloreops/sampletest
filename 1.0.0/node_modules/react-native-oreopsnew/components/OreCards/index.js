import React, {Component} from 'react';
import {TouchableOpacity,StyleSheet,View} from 'react-native';
import OreStyle from '../OreStyle';
export default class OreCards extends Component {
render() {
return (
   
<TouchableOpacity style={[styles.OreCardView,this.props.style]} onPress={this.props.onPress} >
     
      {this.props.cardcomponent}

</TouchableOpacity>

);
}
}

const styles = StyleSheet.create({ 
    OreCardView:{
        marginTop:10,
        padding:10,
        width:"92%",
        marginLeft:"4%",
        borderWidth:2,
        borderColor:"#E0E0E0",
        borderRadius:15,
        marginBottom:"3%",
        backgroundColor:OreStyle.backgroundColor,
        height:100,
        
    },
})